docker run -it --rm debian /bin/bash;

# These commands are to be run inside of the debian container
# apt-get update;
# apt-get install build-essential;
# apt-get install manpages-dev;
# apt-get install vim;
# apt-get install git;
